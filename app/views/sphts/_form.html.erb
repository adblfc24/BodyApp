<%= form_for(@spht) do |f| %>
  <% if @spht.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spht.errors.count, "error") %> prohibited this spht from being saved:</h2>

      <ul>
      <% @spht.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :file %><br>
    <%= f.file_field :file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
